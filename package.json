{
  "name": "caremind-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "emulator": "firebase emulators:start --import=./emulator-data --export-on-exit=./emulator-data",
    "emulator:reset": "rm -rf ./emulator-data && firebase emulators:start --import=./emulator-data --export-on-exit=./emulator-data"
  }
}
